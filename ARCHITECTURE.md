# الهيكل المعماري لتطبيق 3MCode Shop

<p align="center">
  <img src="assets/logo/logo.svg" alt="3MCode Shop Logo" width="150"/>
</p>

<p align="center">
  <a href="#overview">نظرة عامة</a> •
  <a href="#architecture-pattern">نمط الهندسة المعمارية</a> •
  <a href="#project-structure">هيكل المشروع</a> •
  <a href="#data-flow">تدفق البيانات</a> •
  <a href="#dependencies">التبعيات الرئيسية</a> •
  <a href="#diagrams">المخططات</a>
</p>

## <a id="overview"></a>نظرة عامة

يتبع تطبيق 3MCode Shop نمط هندسة معمارية نظيفة (Clean Architecture) مع تطبيق مبدأ فصل المسؤوليات (Separation of Concerns). تم تصميم الهيكل المعماري للتطبيق بحيث يكون:

- **قابل للاختبار**: يمكن اختبار كل طبقة بشكل مستقل.
- **قابل للصيانة**: يمكن تعديل أي جزء من التطبيق دون التأثير على الأجزاء الأخرى.
- **قابل للتوسع**: يمكن إضافة ميزات جديدة بسهولة.
- **مستقل عن الإطار**: لا يعتمد على إطار عمل Flutter بشكل مباشر في طبقات المنطق والبيانات.

## <a id="architecture-pattern"></a>نمط الهندسة المعمارية

### الطبقات الرئيسية

يتكون التطبيق من ثلاث طبقات رئيسية:

#### 1. طبقة العرض (Presentation Layer)

تتضمن كل ما يتعلق بواجهة المستخدم وتفاعله مع التطبيق:

- **الشاشات (Screens)**: تمثل صفحات التطبيق المختلفة.
- **المكونات (Widgets)**: مكونات واجهة المستخدم القابلة لإعادة الاستخدام.
- **BLoCs**: مكونات إدارة حالة التطبيق التي تتوسط بين واجهة المستخدم والطبقات الأخرى.

#### 2. طبقة المنطق (Domain Layer)

تتضمن منطق الأعمال والقواعد الخاصة بالتطبيق:

- **حالات الاستخدام (Use Cases)**: تمثل العمليات التي يمكن للمستخدم القيام بها.
- **الكيانات (Entities)**: تمثل الكائنات الأساسية في التطبيق.
- **المستودعات المجردة (Repository Interfaces)**: تحدد العقود التي يجب أن تلتزم بها المستودعات.

#### 3. طبقة البيانات (Data Layer)

تتضمن كل ما يتعلق بالوصول إلى البيانات ومعالجتها:

- **المستودعات (Repositories)**: تنفيذ واجهات المستودعات المحددة في طبقة المنطق.
- **مصادر البيانات (Data Sources)**: تمثل مصادر البيانات المختلفة (محلية، بعيدة).
- **النماذج (Models)**: تمثل هياكل البيانات المستخدمة في التطبيق.

### نمط BLoC لإدارة الحالة

يستخدم التطبيق نمط BLoC (Business Logic Component) لإدارة حالة التطبيق، حيث:

- **الأحداث (Events)**: تُرسل من واجهة المستخدم إلى BLoC.
- **الحالات (States)**: تُنتج من BLoC وتُستخدم لتحديث واجهة المستخدم.
- **BLoC**: يتلقى الأحداث ويعالجها وينتج حالات جديدة.

## <a id="project-structure"></a>هيكل المشروع

```
lib/
├── core/                   # المكونات الأساسية
│   ├── constants/          # الثوابت
│   │   ├── app_constants.dart
│   │   └── assets_paths.dart
│   ├── localization/       # التعريب والترجمة
│   │   ├── app_localizations.dart
│   │   └── language_manager.dart
│   └── theme/              # سمات التطبيق
│       ├── app_theme.dart
│       └── theme_manager.dart
├── data/                   # طبقة البيانات
│   ├── datasources/        # مصادر البيانات
│   │   ├── local/          # مصادر البيانات المحلية
│   │   │   └── local_data.dart
│   │   └── remote/         # مصادر البيانات البعيدة (مستقبلاً)
│   ├── models/             # نماذج البيانات
│   │   ├── models.dart     # تصدير جميع النماذج
│   │   ├── category_model.dart
│   │   ├── product_model.dart
│   │   ├── cart_item_model.dart
│   │   ├── user_model.dart
│   │   ├── order_model.dart
│   │   ├── address_model.dart
│   │   └── payment_method_model.dart
│   ├── repositories/       # المستودعات
│   │   ├── repositories.dart  # تصدير جميع المستودعات
│   │   ├── category_repository.dart
│   │   ├── product_repository.dart
│   │   ├── cart_repository.dart
│   │   └── user_repository.dart
│   └── services/           # الخدمات
│       ├── user_service.dart
│       └── storage_service.dart
├── presentation/           # طبقة العرض
│   ├── blocs/              # مكونات إدارة الحالة (BLoC)
│   │   ├── blocs.dart      # تصدير جميع BLoCs
│   │   ├── product/        # BLoC للمنتجات
│   │   │   ├── product_bloc.dart
│   │   │   ├── product_event.dart
│   │   │   └── product_state.dart
│   │   ├── category/       # BLoC للتصنيفات
│   │   ├── cart/           # BLoC لسلة التسوق
│   │   ├── auth/           # BLoC للمصادقة
│   │   ├── language/       # BLoC للغة
│   │   └── theme/          # BLoC للسمة
│   ├── screens/            # الشاشات
│   │   ├── screens.dart    # تصدير جميع الشاشات
│   │   ├── main_screen.dart
│   │   ├── home/           # شاشات الصفحة الرئيسية
│   │   ├── product/        # شاشات المنتجات
│   │   ├── cart/           # شاشات سلة التسوق
│   │   ├── checkout/       # شاشات الدفع
│   │   ├── orders/         # شاشات الطلبات
│   │   ├── account/        # شاشات الحساب
│   │   ├── auth/           # شاشات المصادقة
│   │   ├── settings/       # شاشات الإعدادات
│   │   └── splash/         # شاشة البداية
│   └── widgets/            # المكونات المرئية
│       ├── widgets.dart    # تصدير جميع المكونات
│       ├── product_card.dart
│       ├── category_item.dart
│       ├── cart_item_card.dart
│       └── animated_button.dart
├── app.dart                # تكوين التطبيق
└── main.dart               # نقطة الدخول للتطبيق
```

## <a id="data-flow"></a>تدفق البيانات

### 1. تدفق البيانات العام

```
واجهة المستخدم (UI) -> أحداث (Events) -> BLoC -> مستودعات (Repositories) -> مصادر البيانات (Data Sources) -> نماذج (Models)
```

### 2. مثال على تدفق البيانات: تحميل المنتجات

1. **واجهة المستخدم**: تعرض شاشة المنتجات.
2. **الحدث**: يتم إرسال حدث `LoadProducts` إلى `ProductBloc`.
3. **BLoC**: يتلقى `ProductBloc` الحدث ويطلب البيانات من `ProductRepository`.
4. **المستودع**: يجلب `ProductRepository` البيانات من `LocalData`.
5. **مصدر البيانات**: يوفر `LocalData` قائمة المنتجات.
6. **النماذج**: يتم تحويل البيانات إلى كائنات `ProductModel`.
7. **BLoC**: ينتج `ProductBloc` حالة `ProductLoaded` مع قائمة المنتجات.
8. **واجهة المستخدم**: تتحدث الشاشة لعرض المنتجات.

### 3. مثال على تدفق البيانات: إضافة منتج إلى سلة التسوق

1. **واجهة المستخدم**: يضغط المستخدم على زر "أضف إلى السلة".
2. **الحدث**: يتم إرسال حدث `AddToCart` إلى `CartBloc`.
3. **BLoC**: يتلقى `CartBloc` الحدث ويطلب إضافة المنتج من `CartRepository`.
4. **المستودع**: يضيف `CartRepository` المنتج إلى سلة التسوق في `LocalData`.
5. **مصدر البيانات**: يحفظ `LocalData` المنتج في التخزين المحلي.
6. **BLoC**: ينتج `CartBloc` حالة `CartLoaded` مع قائمة المنتجات المحدثة.
7. **واجهة المستخدم**: تتحدث الشاشة لعرض رسالة تأكيد وتحديث عداد السلة.

## <a id="dependencies"></a>التبعيات الرئيسية

### إدارة الحالة
- **flutter_bloc**: لإدارة حالة التطبيق باستخدام نمط BLoC.
- **equatable**: لتسهيل مقارنة الكائنات وتحسين أداء BLoC.
- **bloc_concurrency**: لإدارة التزامن في BLoC.

### التخزين المحلي
- **shared_preferences**: لتخزين البيانات البسيطة مثل إعدادات اللغة والسمة.
- **hive** و **hive_flutter**: لتخزين البيانات المعقدة مثل سلة التسوق والمنتجات.

### واجهة المستخدم
- **flutter_svg**: لعرض الصور بتنسيق SVG.
- **carousel_slider**: لعرض البانرات المتحركة.
- **image_picker**: لاختيار الصور من معرض الجهاز أو الكاميرا.

### التعريب والترجمة
- **flutter_localizations**: لدعم التعريب والترجمة.
- **intl**: لتنسيق التواريخ والأرقام حسب اللغة.

## <a id="diagrams"></a>المخططات

### مخطط الطبقات

```
┌─────────────────────────────────────────────────────────────┐
│                     طبقة العرض (Presentation)               │
│                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐      │
│  │   الشاشات   │    │    BLoCs    │    │   المكونات  │      │
│  │  (Screens)  │    │   (BLoCs)   │    │  (Widgets)  │      │
│  └─────────────┘    └─────────────┘    └─────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                     طبقة المنطق (Domain)                    │
│                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐      │
│  │حالات الاستخدام│    │   الكيانات  │    │  المستودعات │      │
│  │ (Use Cases) │    │ (Entities)  │    │ (Interfaces)│      │
│  └─────────────┘    └─────────────┘    └─────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                     طبقة البيانات (Data)                    │
│                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐      │
│  │  المستودعات │    │مصادر البيانات│    │   النماذج   │      │
│  │(Repositories)│    │(Data Sources)│    │  (Models)  │      │
│  └─────────────┘    └─────────────┘    └─────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

### مخطط BLoC

```
┌─────────────────────────────────────────────────────────────┐
│                        واجهة المستخدم                       │
│                           (UI)                              │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            │ أحداث (Events)
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                           BLoC                              │
│                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐      │
│  │   الأحداث   │    │   المعالجة  │    │   الحالات   │      │
│  │  (Events)   │ ─> │ (Processing)│ ─> │  (States)   │      │
│  └─────────────┘    └─────────────┘    └─────────────┘      │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            │ حالات (States)
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                        واجهة المستخدم                       │
│                           (UI)                              │
└─────────────────────────────────────────────────────────────┘
```

### مخطط تدفق البيانات

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  واجهة   │     │          │     │          │     │  مصادر   │
│ المستخدم │ ──> │   BLoC   │ ──> │ المستودع │ ──> │ البيانات │
│   (UI)   │     │  (BLoC)  │     │  (Repo)  │     │  (Data)  │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
     ▲               │                │                │
     │               │                │                │
     └───────────────┴────────────────┴────────────────┘
                      تدفق البيانات
```

---

<p align="center">
  تم التطوير بواسطة <a href="https://www.3mcode.com">3MCode</a> - جميع الحقوق محفوظة © 2025
</p>
