class AppConstants {
  // App general constants
  static const String appName = '3MCode Shop';
  
  // Navigation constants
  static const int homeIndex = 0;
  static const int productsIndex = 1;
  static const int cartIndex = 2;
  static const int aboutIndex = 3;
  
  // Default values
  static const double defaultPadding = 16.0;
  static const double defaultBorderRadius = 10.0;
  
  // Animation durations
  static const Duration defaultAnimationDuration = Duration(milliseconds: 300);
  
  // Mock network delay (to simulate API calls)
  static const Duration mockNetworkDelay = Duration(milliseconds: 800);
}
