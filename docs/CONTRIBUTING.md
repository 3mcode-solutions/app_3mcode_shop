# دليل المساهمة في تطبيق 3MCode Shop

<p align="center">
  <img src="assets/logo/logo.svg" alt="3MCode Shop Logo" width="150"/>
</p>

<p align="center">
  <a href="#introduction">مقدمة</a> •
  <a href="#code-of-conduct">قواعد السلوك</a> •
  <a href="#getting-started">البدء</a> •
  <a href="#development-workflow">سير عمل التطوير</a> •
  <a href="#pull-requests">طلبات السحب</a> •
  <a href="#coding-standards">معايير الترميز</a> •
  <a href="#testing">الاختبار</a> •
  <a href="#documentation">التوثيق</a>
</p>

## <a id="introduction"></a>مقدمة

شكرًا لاهتمامك بالمساهمة في تطبيق 3MCode Shop! هذا الدليل يوضح العملية والمعايير للمساهمة في المشروع. من خلال اتباع هذه الإرشادات، نضمن أن تكون مساهماتك ذات جودة عالية وتتوافق مع أهداف المشروع.

## <a id="code-of-conduct"></a>قواعد السلوك

نحن نتوقع من جميع المساهمين الالتزام بقواعد السلوك التالية:

- احترام جميع المساهمين والمستخدمين.
- قبول النقد البناء والعمل على تحسين المشروع.
- التركيز على ما هو أفضل للمجتمع.
- إظهار التعاطف تجاه أعضاء المجتمع الآخرين.

## <a id="getting-started"></a>البدء

### متطلبات النظام

- Flutter SDK (الإصدار 3.19.3 أو أحدث)
- Dart SDK (الإصدار 3.7.2 أو أحدث)
- Android Studio / VS Code
- Git

### إعداد بيئة التطوير

1. استنساخ المستودع:
   ```bash
   git clone https://github.com/yourusername/app_3mcode_shop.git
   cd app_3mcode_shop
   ```

2. تثبيت التبعيات:
   ```bash
   flutter pub get
   ```

3. التأكد من تكوين Flutter بشكل صحيح:
   ```bash
   flutter doctor
   ```

4. إنشاء فرع جديد للميزة أو الإصلاح:
   ```bash
   git checkout -b feature/your-feature-name
   ```

## <a id="development-workflow"></a>سير عمل التطوير

### 1. تحديد المشكلة أو الميزة

قبل البدء في العمل، تأكد من وجود مشكلة (issue) مفتوحة تصف التغيير الذي ترغب في إجرائه. إذا لم تكن هناك مشكلة موجودة، فقم بإنشاء واحدة جديدة.

### 2. مناقشة الحل

ناقش الحل المقترح في المشكلة للتأكد من أنه يتوافق مع أهداف المشروع وتصميمه. هذا يساعد في تجنب العمل الذي قد لا يتم قبوله.

### 3. تنفيذ التغييرات

1. قم بتحديث فرعك من الفرع الرئيسي:
   ```bash
   git checkout main
   git pull
   git checkout feature/your-feature-name
   git rebase main
   ```

2. قم بتنفيذ التغييرات المطلوبة، مع الالتزام بمعايير الترميز.

3. قم بإضافة اختبارات للتغييرات الجديدة.

4. قم بتحديث التوثيق حسب الحاجة.

### 4. الالتزام بالتغييرات

1. قم بإضافة التغييرات:
   ```bash
   git add .
   ```

2. قم بالالتزام بالتغييرات مع رسالة وصفية:
   ```bash
   git commit -m "feat: add new feature" -m "Description of the changes"
   ```

   نحن نتبع [Conventional Commits](https://www.conventionalcommits.org/) لرسائل الالتزام:
   - `feat`: ميزة جديدة
   - `fix`: إصلاح خطأ
   - `docs`: تغييرات في التوثيق
   - `style`: تغييرات في التنسيق (مسافات، فواصل، إلخ)
   - `refactor`: إعادة هيكلة الكود
   - `test`: إضافة أو تعديل الاختبارات
   - `chore`: تغييرات في عملية البناء أو الأدوات المساعدة

### 5. دفع التغييرات

```bash
git push origin feature/your-feature-name
```

## <a id="pull-requests"></a>طلبات السحب

### إنشاء طلب السحب

1. انتقل إلى صفحة المستودع على GitHub.
2. انقر على "Pull Requests" ثم "New Pull Request".
3. اختر فرعك كفرع المصدر والفرع الرئيسي كفرع الهدف.
4. املأ نموذج طلب السحب:
   - عنوان واضح ووصفي
   - وصف مفصل للتغييرات
   - ربط المشكلة ذات الصلة
   - قائمة بالتغييرات الرئيسية
   - لقطات شاشة أو صور GIF للتغييرات المرئية (إن وجدت)

### مراجعة الكود

سيتم مراجعة طلب السحب الخاص بك من قبل المشرفين على المشروع. قد يطلبون تغييرات أو توضيحات. كن مستعدًا للرد على التعليقات وإجراء التغييرات المطلوبة.

### دمج طلب السحب

بعد الموافقة على طلب السحب، سيقوم المشرفون بدمجه في الفرع الرئيسي. في بعض الحالات، قد يطلبون منك إعادة قاعدة (rebase) الفرع الخاص بك قبل الدمج.

## <a id="coding-standards"></a>معايير الترميز

### أسلوب الكود

نتبع [Effective Dart](https://dart.dev/guides/language/effective-dart) لأسلوب الكود. بالإضافة إلى ذلك، نلتزم بالمبادئ التالية:

#### التسمية

- استخدم `camelCase` لأسماء المتغيرات والدوال.
- استخدم `PascalCase` لأسماء الفئات والمكونات.
- استخدم `snake_case` لأسماء الملفات.
- استخدم `SCREAMING_SNAKE_CASE` للثوابت.

#### التنظيم

- قم بتنظيم الكود في وحدات منطقية.
- استخدم التعليقات لشرح الكود المعقد.
- اجعل الدوال والفئات صغيرة ومركزة.
- اتبع مبدأ المسؤولية الواحدة (SRP).

### هيكل المشروع

اتبع هيكل المشروع الموضح في [وثيقة الهيكل المعماري](ARCHITECTURE.md).

### التعليقات

- استخدم تعليقات Dart Doc للفئات والدوال العامة.
- اشرح لماذا (وليس كيف) في التعليقات.
- حافظ على التعليقات محدثة مع الكود.

## <a id="testing"></a>الاختبار

### أنواع الاختبارات

- **اختبارات الوحدة**: اختبار وحدات الكود المنفصلة.
- **اختبارات التكامل**: اختبار تفاعل الوحدات مع بعضها البعض.
- **اختبارات واجهة المستخدم**: اختبار واجهة المستخدم والتفاعلات.

### كتابة الاختبارات

- اكتب اختبارات لكل ميزة جديدة أو إصلاح.
- استخدم أسماء وصفية للاختبارات.
- اتبع نمط "Given-When-Then" في اختبارات الوحدة.
- استخدم البيانات الوهمية (mocks) عند الحاجة.

### تشغيل الاختبارات

```bash
flutter test
```

## <a id="documentation"></a>التوثيق

### توثيق الكود

- استخدم تعليقات Dart Doc للفئات والدوال العامة.
- اشرح الغرض والاستخدام والمعلمات والقيم المرجعة.

```dart
/// يقوم بإضافة منتج إلى سلة التسوق.
///
/// يتطلب [product] المنتج المراد إضافته و [quantity] الكمية المطلوبة.
/// يرجع `true` إذا تمت الإضافة بنجاح، و `false` إذا فشلت.
bool addToCart(ProductModel product, {int quantity = 1}) {
  // ...
}
```

### توثيق المشروع

- قم بتحديث ملفات التوثيق (README.md، ARCHITECTURE.md، إلخ) عند إجراء تغييرات كبيرة.
- أضف تعليمات الاستخدام للميزات الجديدة.
- قم بتوثيق التغييرات المهمة في واجهة برمجة التطبيقات (API).

## الإبلاغ عن المشكلات

إذا وجدت مشكلة في التطبيق، يرجى الإبلاغ عنها باستخدام نظام تتبع المشكلات في GitHub. تأكد من تضمين المعلومات التالية:

- وصف واضح وموجز للمشكلة
- خطوات إعادة إنتاج المشكلة
- السلوك المتوقع والسلوك الفعلي
- لقطات شاشة أو سجلات الأخطاء (إن وجدت)
- معلومات النظام (إصدار Flutter، نظام التشغيل، إلخ)

## طلب الميزات

إذا كان لديك اقتراح لميزة جديدة، يرجى إنشاء مشكلة جديدة باستخدام قالب طلب الميزة. تأكد من تضمين المعلومات التالية:

- وصف واضح وموجز للميزة
- شرح لماذا تعتقد أن هذه الميزة ستكون مفيدة
- أمثلة على كيفية استخدام الميزة
- أي موارد أو مراجع ذات صلة

## الاتصال

إذا كان لديك أي أسئلة أو استفسارات، يمكنك التواصل معنا عبر:

- البريد الإلكتروني: support@3mcode-shop.com
- قناة Slack: [3MCode Shop Slack](https://3mcode-shop.slack.com)
- Twitter: [@3MCodeShop](https://twitter.com/3MCodeShop)

---

<p align="center">
  تم التطوير بواسطة <a href="https://www.3mcode.com">3MCode</a> - جميع الحقوق محفوظة © 2025
</p>
